import { expect } from 'chai';

describe('checkLength', () => {
  it('returns an empty array for both colours for an empty board');

  it('returns an array of one coord for a board with a single token');

  it(
    'returns an array of one coord each for a board with two different tokens'
  );
});
