import { expect } from 'chai';

describe('checkWin', () => {
  it('should accept an array of arrays representing a board');
  it('should return the winning colour');
  it('should return the coordinates of the winning line');
});
